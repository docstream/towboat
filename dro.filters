# arg1 = region-code (str)
region(){
  awk "\$4 ~ /$1/ {print}" 
}

priv_ips(){
  egrep -o "private;[0-9\.]*" \
    | sed 's|[^;]*;||' 
}

publ_ips(){
  egrep -o "public;[0-9\.]*" \
    | sed 's|[^;]*;||' 
}

json_str_list(){
  awk '{ print "\"" $0 "\"" }' \
  | jq -s -c "."
}
